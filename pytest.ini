[pytest]
markers =
    live: makes real Anthropic API calls and costs money; skipped unless REE_ASSISTANT_LIVE_TESTS=1

# The default run is the free one. Live tests additionally gate themselves on
# the env var, so `pytest tests/` stays free even if this is overridden.
addopts = -m "not live"
